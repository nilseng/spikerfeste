{
  "name": "spikerfeste",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.ts",
  "scripts": {
    "heroku-prebuild": "npm i && cd client && npm i && npm run build && cd .. && cd server && npm i && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^11.3.2"
  }
}
